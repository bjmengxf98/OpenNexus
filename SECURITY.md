# 安全策略

[简体中文](SECURITY.md) · [English](SECURITY_EN.md)

## 报告漏洞

请不要在公开 Issue 中披露可利用细节、真实凭据或用户数据。优先使用 GitHub 仓库的 **Private vulnerability reporting / Security advisory** 私下报告，并说明受影响版本、复现方法与影响范围。

## 支持范围

安全修复以主分支的最新版本为准。旧部署应先升级到最新版本再验证问题。

## 部署要求

- 使用独立的强随机 `SESSION_SECRET`，生产环境启用 HTTPS 和安全 Cookie。
- `.env`、SQLite 数据库、备份、日志、上传文件和二维码不得公开。
- 对 WPS、模型、邮件、MCP 与微信令牌遵循最小权限，并定期轮换。
- MCP 令牌只显示一次，不要通过聊天截图或公开渠道传递。
- 仅允许可信网络访问管理后台和数据库备份。
- 个人微信桥接属于实验性功能；生产启用前应评估平台规则、账号和隐私风险。

如果凭据曾被提交到 Git 历史或公开附件中，应立即撤销/轮换，并清理完整历史；只在后续提交中删除文件不能消除泄露。
