# Runtime data directory

OpenNexus creates its SQLite database and runtime state in this directory.

Database files, backups, tokens, chat history and user uploads are deliberately
excluded from version control. Do not commit production data to a public
repository.
