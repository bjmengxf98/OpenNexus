# OpenNexus 稳定版维护约定

## 项目角色

本目录 `D:\python-code\业务创新1\部门管理助手` 是当前仍在使用和部署的 OpenNexus 稳定版。允许继续修复问题和完善必要功能，但必须优先保证生产可用性和向后兼容。

下一代重建项目位于：

`D:\python-code\业务创新1\OpenNexus-Next`

两个项目并行期间，本项目是“稳定版维护线”，新项目是“架构重建线”。不得为了方便而把新架构的半成品反向覆盖稳定版。

## 开始工作前

涉及功能修改的对话必须先阅读：

1. `docs/NEXT_MIGRATION_CHANGELOG.md`
2. `CHANGELOG.md`
3. 与任务直接相关的现有说明和测试

## 稳定版修改原则

- 可以继续修复缺陷、改善界面和新增确有需要的业务功能；
- 优先小范围、可验证、可回滚的修改，不在稳定版开展大规模架构重建；
- 不得因新项目存在而拒绝必要的稳定版维护；
- 不得因为稳定版临时实现较快，就省略权限、幂等、写后核验和错误处理；
- `app.py`、`agent/assistant.py` 的必要修改可以进行，但必须特别记录行为变化，供新项目按新架构重新实现；
- 未经用户明确授权，不得部署服务器、合并 `main` 或修改生产数据。

## 每次功能修改必须留下的迁移记录

凡是共同基线之后可能影响新项目的修改，在结束前必须追加到 `docs/NEXT_MIGRATION_CHANGELOG.md`，至少包括：

- 唯一变更编号：`STABLE-YYYYMMDD-NN`；
- 日期、分支、提交号或待提交状态；
- 用户可感知的行为变化；
- 涉及的代码、页面、数据库、API、配置和依赖；
- 权限、副作用、兼容性和部署注意事项；
- 验证步骤及结果；
- 建议在新项目中直接移植、重新实现或判定不适用。

仅修改代码而不登记迁移记录，任务不算完整。纯注释、纯格式化等明确不影响行为的修改可以不登记，但应在提交说明中写清楚。

## 与新项目同步的边界

- 稳定版只负责准确记录变化，不直接假定新项目已经同步；
- 简单且架构无关的修复可建议新项目 `cherry-pick`；
- 涉及 `app.py`、`agent/assistant.py`、工具循环、上下文、权限或集成流程的变化，原则上由新项目按新版边界重新实现；
- 数据库、API、配置格式和外部系统行为的改变必须标为高优先级同步项；
- 新项目只有在完成代码迁移、契约测试和行为对照后，才能把记录状态改为“已验收”。

## 最终切换前

不能只看修改日志。必须由新项目对共同基线之后的 Git 提交、代码差异、数据库迁移、API、配置、依赖和黄金任务进行完整审计，确认没有漏项后才能切换。
