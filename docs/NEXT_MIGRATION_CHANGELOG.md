# 稳定版 → OpenNexus Next 迁移变更账本

## 1. 用途

本文件记录稳定版在 OpenNexus Next 重建期间发生的、可能需要同步到新版的全部行为变化。它是跨项目协作账本，不替代 Git 提交、测试或正式 `CHANGELOG.md`。

- 稳定版目录：`D:\python-code\业务创新1\部门管理助手`
- 新项目目录：`D:\python-code\业务创新1\OpenNexus-Next`
- 当前共同基线提交：`02c78f88b4a63cbf1d4684800b8f9f0dd7b5f3e0`
- 基线分支：`codex/main-release-20260827`
- 建账日期：2026-08-28

共同基线之后的稳定版变化必须登记。若基线重新确定，必须新增记录说明，不能直接覆盖历史基线。

## 2. 状态定义

| 状态 | 含义 |
|---|---|
| `待提交` | 稳定版代码已修改，但尚无最终提交号 |
| `待评估` | 稳定版已提交，新项目尚未决定迁移方式 |
| `待迁移` | 已确认新版需要实现 |
| `迁移中` | 新项目已经开始处理 |
| `待验收` | 新版实现完成，尚未完成行为对照 |
| `已验收` | 新版通过契约测试和行为对照 |
| `不适用` | 有明确理由不进入新版，并已记录决定 |

稳定版维护对话只能创建记录、补充稳定版提交号和验证结果。只有新项目完成迁移验证后，才能将状态改为 `已验收`。

## 3. 迁移优先级

- `P0`：安全、数据损坏、权限、生产阻断修复；
- `P1`：数据库/API/配置变化、核心业务功能、外部集成协议；
- `P2`：普通功能、兼容性和重要界面行为；
- `P3`：不影响业务语义的体验改善。

## 4. 变更索引

| 变更编号 | 日期 | 稳定版提交 | 优先级 | 简述 | 新版状态 | 新版实现/提交 |
|---|---|---|---|---|---|---|
| BASELINE-20260828 | 2026-08-28 | `02c78f88` | — | 建立双轨同步共同基线 | 已验收 | 新项目由此副本开始 |
| STABLE-20260828-01 | 2026-08-28 | `60b8373` | P1 | 任务真实写入门禁与微信桥接可选降级 | 待迁移 | 待定 |
| STABLE-20260828-02 | 2026-08-28 | `151f392` | P3 | 手机 PWA 新对话按钮移至功能区与对话列表之间 | 待迁移 | 待定 |
| STABLE-20260830-01 | 2026-08-30 | `95f158d` | P2 | 左侧会话列表支持安全的批量选择与原子删除 | 待迁移 | 待定 |
| STABLE-20260830-02 | 2026-08-30 | `a0547a1` | P1 | 提醒查询事实门禁、通知话术降级与表格查询策略纠偏 | 待评估 | 待定 |
| STABLE-20260830-03 | 2026-08-30 | `612275b` | P3 | 对话 Token 信息分界线增强 | 待迁移 | 待定 |
| STABLE-20260911-01 | 2026-09-11 | 待提交 | P1 | Word 文档结构校验、错误上传门禁与模型重试回执 | 待迁移 | 待定 |
| STABLE-20260911-02 | 2026-09-11 | `e20bcb3` | P1 | DeepSeek V4.1 模型、长上下文、多模态与空白回复保护 | 待迁移 | 待定 |
| STABLE-20260914-01 | 2026-09-14 | `7579efa` | P2 | 产品中文名称统一为“业务智能助手” | 待评估 | 待定 |
| STABLE-20260915-01 | 2026-09-15 | `342ba4f` | P2 | 驾驶舱改称“业务智能驾驶舱”并泛化报告文案 | 待评估 | 待定 |
| STABLE-20260915-02 | 2026-09-15 | `342ba4f` | P1 | 驾驶舱按任意业务表格实际字段生成概览 | 待迁移 | 待定 |
| STABLE-20260915-03 | 2026-09-15 | `342ba4f` | P1 | 驾驶舱空日期快照校验与按需读取 | 待迁移 | 待定 |
| STABLE-20260915-04 | 2026-09-15 | `342ba4f` | P1 | WPS 非数据工作表兼容与切表失败隔离 | 待迁移 | 待定 |
| STABLE-20260915-05 | 2026-09-15 | `342ba4f` | P1 | 大模型声明式设计任意业务驾驶舱 | 待迁移 | 待定 |
| STABLE-20260915-06 | 2026-09-15 | `342ba4f` | P2 | 不定数量驾驶舱图表响应式均衡排列 | 待迁移 | 待定 |
| STABLE-20260915-07 | 2026-09-15 | `342ba4f` | P1 | 驾驶舱字段语义门禁、关联值解码与核心指标精简 | 待迁移 | 待定 |
| STABLE-20260916-01 | 2026-09-16 | `0380904` | P1 | 模型相对日期与星期权威日历注入 | 待迁移 | 待定 |
| STABLE-20260916-02 | 2026-09-16 | 待提交 | P1 | 提醒过期门禁与有限重试 | 待迁移 | 待定 |
| STABLE-20260916-03 | 2026-09-16 | 待提交 | P1 | 个人微信多部署单一连接与桥接隔离 | 待迁移 | 待定 |
| STABLE-20260917-01 | 2026-09-17 | 待提交 | P1 | 个人微信会话激活、上下文持久化与主动发送真值门禁 | 待迁移 | 待定 |

## 5. 单项记录模板

复制以下模板追加记录，不得删除已验收历史：

```markdown
### STABLE-YYYYMMDD-NN：简短标题

- 日期：YYYY-MM-DD
- 稳定版分支：
- 稳定版提交：待提交 / commit SHA
- 优先级：P0 / P1 / P2 / P3
- 新版状态：待评估
- 修改原因：
- 用户可见行为：
- 涉及文件：
- 数据库变化：无 / 详细说明
- API 或事件协议变化：无 / 详细说明
- 配置、环境变量或依赖变化：无 / 详细说明
- 权限、副作用和兼容性：
- 稳定版验证：测试命令、场景和结果
- 建议迁移方式：直接移植 / 按新版架构重新实现 / 待评估
- 新版落点建议：
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：
```

## 6. 登记判断

以下情况必须登记：

- 新增、删除或改变用户可见功能；
- 修复会影响业务结果的缺陷；
- 修改工具参数、返回值、完成条件或重试逻辑；
- 修改 WPS、微信、OA、提醒、文档、表格或模型调用行为；
- 修改数据库表、字段、索引、迁移脚本或数据语义；
- 修改 API、Webhook、事件、环境变量、依赖或部署方式；
- 修改权限、确认、幂等、审计或错误降级策略；
- 改变 Token、上下文、工具选择和任务规划行为。

纯文字纠错或内部重命名只有在确认行为、接口、配置和数据都不变时才可不登记。

## 7. 已登记变更

### STABLE-20260828-01：任务真实写入门禁与微信桥接可选降级

- 日期：2026-08-28
- 稳定版分支：`codex/main-release-20260827`（已发布到 `main`）
- 稳定版提交：`60b8373`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：模型曾在没有调用 `create_records` 时自行声称 WPS 返回 403；可选的个人微信桥接退出后又被固定每 30 秒重启，既缺少退出日志，又会无限产生进程和日志噪声。
- 用户可见行为：明确的“建立任务”请求只有在实际执行写入并回读确认后才会报告成功；真实工具错误会按工具回执说明，不再编造 403。微信桥接不可用时主系统继续工作，系统按指数退避尝试恢复，连续失败后暂停自动重启，并保留独立诊断日志。
- 涉及文件：`agent/assistant.py`、`app.py`、`core/wechat_supervisor.py`、`test_tool_planner.py`、`test_reminders.py`、`test_wechat_supervisor.py`、`CHANGELOG.md`
- 数据库变化：无
- API 或事件协议变化：无；沿用现有 WPS 工具和微信桥接接口。
- 配置、环境变量或依赖变化：无新增依赖；沿用 `WECHAT_SHOW_LOG`。默认将桥接子进程输出保存到 `logs/wechat-bridge-<account_id>.log`，达到约 2 MB 时保留一份轮转日志。
- 权限、副作用和兼容性：不改变原有任务字段、WPS 权限或微信启用方式；微信仍为可选能力。写入已成功但回读暂时失败时不会重复创建同一任务，而会如实提示“已写入、尚未完成回读确认”。
- 稳定版验证：覆盖主程序、智能体、提醒、工具治理、任务写入门禁和微信桥接的最终回归测试共 77 项通过；`app.py`、`agent/assistant.py`、`core/wechat_supervisor.py` 及相关测试均通过 `py_compile`。仓库原有联网调试脚本仍需外部凭证或不符合 pytest 收集约定，与本次修改无关。
- 建议迁移方式：按新版架构重新实现，不直接复制稳定版大型运行循环。
- 新版落点建议：将“写工具回执 + 回读验证 + 完成声明”实现为统一运行时完成策略；将微信等可选集成实现为带健康状态、退避和熔断的独立适配器。对应新版 Harness 原则 H06、H12、H15。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：迁移时应保留“任务完成以真实副作用为准”和“可选集成失败不影响核心服务”两项行为契约。

### STABLE-20260828-02：手机 PWA 新对话按钮归位

- 日期：2026-08-28
- 稳定版分支：`codex/main-release-20260827`（发布目标为 `main`）
- 稳定版提交：`151f392`
- 优先级：P3
- 新版状态：待迁移
- 修改原因：手机侧边栏的“新对话”按钮原先位于功能菜单上方，与“功能区结束后进入对话区”的信息层级不一致。
- 用户可见行为：手机端侧边栏现在依次显示功能菜单、“新对话”按钮和历史对话列表；按钮紧邻对话列表并位于第一条对话上方。桌面端功能菜单保持隐藏，因此桌面侧边栏视觉顺序不变。
- 涉及文件：`static/app_new.html`、`test_app_new.py`、`CHANGELOG.md`
- 数据库变化：无
- API 或事件协议变化：无
- 配置、环境变量或依赖变化：无
- 权限、副作用和兼容性：仅调整既有 DOM 元素顺序，不改变元素 ID、点击事件、对话创建接口或移动端遮罩行为。
- 稳定版验证：`C:\Python314\python.exe -m pytest -q -p no:cacheprovider test_app_new.py`，10 项通过；新增测试固定“手机功能区 < 新对话 < 对话列表”的结构顺序。
- 建议迁移方式：按新版界面组件结构直接实现相同的信息层级。
- 新版落点建议：新版移动端会话抽屉中的操作区与会话列表组件之间。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：验收时同时检查窄屏、深色主题和桌面侧边栏，不应因为 DOM 调整产生重复按钮。

### STABLE-20260830-01：左侧会话批量删除

- 日期：2026-08-30
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`95f158d`
- 优先级：P2
- 新版状态：待迁移
- 修改原因：历史对话较多时只能逐条打开菜单删除，整理成本高；简单地从前端循环调用单条删除还会产生部分成功、部分失败的数据不一致风险。
- 用户可见行为：左侧会话栏新增“批量管理”，进入后可逐条勾选、全选或取消全选，删除按钮实时显示数量；执行前明确提示会同时永久删除消息和任务记录且无法恢复。当前页面正在回复时不能进入批量模式，任一选中对话存在运行中任务时整批拒绝删除。
- 涉及文件：`static/app_new.html`、`api/app_new_routes.py`、`app.py`、`auth/db.py`、`test_app_new.py`、`test_agent_turns.py`、`CHANGELOG.md`
- 数据库变化：无表结构变化；删除事务现在同时清理选中对话对应的 `chat_history`、会话级 `memory_items`、`agent_turns` 和 `agent_turn_events`，并严格限定当前用户。
- API 或事件协议变化：新增 `POST /api/app-new/conversations/batch-delete`，请求体为 `{"conversation_ids":[整数...]}`，成功返回 `deleted_ids` 和 `deleted_count`；空数组或非法编号返回 400，非本人或不存在的对话返回 404，存在运行中任务返回 409。原单条删除接口补充同类 404/409 错误响应。
- 配置、环境变量或依赖变化：无。
- 权限、副作用和兼容性：最多一次处理 500 个编号；服务端先完整验证所有权和运行状态，再在同一 SQLite 事务中删除，任何一项校验失败都不会删除其中任何对话。删除不可恢复，因此前端保留明确二次确认；原有单条重命名、删除和新对话功能继续可用。
- 稳定版验证：`python -m pytest test_app_new.py test_agent_turns.py -q`，20 项通过；正式回归集中 117 项通过，另有既存 `test_document_gen.py` 因环境未安装异步 pytest 插件而失败，与本次修改无关；`app.py`、`api/app_new_routes.py`、`auth/db.py` 通过 `py_compile`；页面 3 个内联脚本通过 Node 语法检查。
- 建议迁移方式：按新版架构重新实现，不直接复制稳定版 HTML 内联脚本或数据库函数。
- 新版落点建议：在会话应用服务中实现 owner-scoped 的 `DeleteConversations` 用例和事务边界，前端会话抽屉实现独立批量选择状态；以契约测试固定所有权、活动任务冲突、全有或全无删除及删除后当前会话切换行为。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：该功能属于破坏性数据操作，新版不得退化为前端逐条调用或缺少二次确认。

### STABLE-20260830-02：提醒查询事实门禁与工具查询策略纠偏

- 日期：2026-08-30
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`a0547a1`
- 优先级：P1
- 新版状态：待评估
- 修改原因：parity 第四轮实测中，弱模型面对“我有哪些提醒？”没有看到 `list_reminders`，错误声称查询接口未开放；另有特定人员查询和变更查询重复读取未筛选全表、浪费 Token，以及提醒成功回复承诺个人微信必然送达的问题。
- 用户可见行为：查看提醒的常见说法会在首轮直接暴露并优先调用 `list_reminders`；成功读取后由系统根据当前用户数据库记录生成回复，模型不能再把真实结果改写成“能力未开放”。提醒创建成功时明确说明已保存并会通过届时已启用且可用的通道尝试推送，不保证可选微信桥接必然在线。特定人员、部门、项目、任务或时间查询若先读取未筛选全表，会收到一次使用精确 `filter` 的策略提示；其他任务连续三次未筛选读取后收到一次改用 `filter`/`analyze_records` 的提示，但不设置固定工具轮数，也不提前结束任务。
- 涉及文件：`agent/tool_planner.py`、`agent/assistant.py`、`core/mcp_server.py`、`test_tool_planner.py`、`test_reminders.py`、`CHANGELOG.md`
- 数据库变化：无；继续读取现有 `reminders` 表，并保持按当前用户 `user_id` 隔离。
- API 或事件协议变化：无新增端点；`add_reminder` 的聊天工具和 MCP 成功消息改为“已设置并保存、尝试推送”，不再承诺一定送达。`list_records` 的成功工具结果在命中低效查询时可能新增内部 `_strategy_hint` 字段，仅供本轮模型调整策略。
- 配置、环境变量或依赖变化：无。
- 权限、副作用和兼容性：提醒列表仍只读取当前用户数据；策略提示不修改模型参数、不自动改写查询条件、不限制工具次数。提醒创建的数据库写入和调度逻辑未改变，只收紧成功事实与通知通道表述。
- 稳定版验证：`C:\Python314\python.exe -m pytest -q test_tool_planner.py test_reminders.py`，专项 49 项通过；完整非文档生成回归集 123 项通过。`agent/assistant.py`、`agent/tool_planner.py`、`core/mcp_server.py` 及两份测试通过 `py_compile`。新增回归覆盖弱模型错误否定能力、真实提醒列表覆盖模型答复、提醒查询首轮路由、通知话术和非终止式查询策略提示。
- 建议迁移方式：按新版架构重新实现，不直接复制稳定版 `assistant.py` 工具循环。
- 新版落点建议：将“查询意图 → 能力路由 → 工具事实回执 → 确定性呈现”实现为统一只读事实策略；将通知通道健康状态作为提醒回执的一部分；将重复无进展检测实现为可观测的策略调整信号而非固定轮数终止器。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：证据来自 `parity/reports/parity_report-20260830-105633.json` 的 S-C03，以及双跑问题清单 O-01/O-02/O-03。该记录只描述稳定版变化，不表示新项目已同步。

### STABLE-20260830-03：对话 Token 信息分界线增强

- 日期：2026-08-30
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`612275b`
- 优先级：P3
- 新版状态：待迁移
- 修改原因：助手回复底部的 Token、模型轮数和工具次数统计使用全局浅色分界线，在浅色背景下对比度不足，难以辨认信息区边界。
- 用户可见行为：Token 统计区域上方仍保持原有 1px 虚线样式，但改用独立的较深分界线颜色；浅色和深色主题分别设置合适对比度，不影响正文、表格或输入区边线。
- 涉及文件：`static/app_new.html`、`test_app_new.py`、`CHANGELOG.md`
- 数据库变化：无。
- API 或事件协议变化：无。
- 配置、环境变量或依赖变化：无。
- 权限、副作用和兼容性：纯 CSS 视觉调整；不改变 Token 统计内容、持久化数据、消息 DOM 结构或交互逻辑。
- 稳定版验证：`C:\Python314\python.exe -m pytest -q -p no:cacheprovider test_app_new.py`，11 项全部通过；`test_app_new.py` 通过 `py_compile`。
- 建议迁移方式：在新版消息元数据组件中直接采用等价的主题色变量，不复制其他稳定版页面结构。
- 新版落点建议：助手消息的运行指标/Token 元数据组件边框主题。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：仅提升视觉可辨识度，不改变 Token 用量计算和展示口径。

### STABLE-20260911-01：Word 文档结构校验与错误上传门禁

- 日期：2026-09-11
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`e20bcb3`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：弱模型偶尔把 `generate_document.content` 序列化成包含未转义内部引号的字符串；稳定版旧逻辑在 JSON 解析失败后按行转成普通段落并继续上传，导致 Word 正文直接显示 `{"sections": ...}` 源码，同时界面仍报告生成成功。
- 用户可见行为：合法的对象、JSON 字符串、Markdown JSON 代码块、双重序列化字符串和带前置说明的 JSON 仍可生成；无法可靠解析或结构不合法时不创建、不上传文件，工具返回可重试错误，模型必须修正 `content` 后再次调用且未成功前不得宣称完成。
- 涉及文件：`core/document_generator.py`、`agent/assistant.py`、`test_document_generator.py`、`CHANGELOG.md`、`DEVLOG.md`
- 数据库变化：无。
- API 或事件协议变化：`generate_document` 失败结果新增稳定字段 `code=invalid_document_structure`、`retryable=true`、`uploaded=false`；成功结果格式不变。
- 配置、环境变量或依赖变化：无新增依赖；继续使用项目已有的 `python-docx`。
- 权限、副作用和兼容性：上传前执行结构校验，失败路径不触发 WPS 写入；不修改 WPS 授权、文件目录或正常模板。未知 section 类型由原先静默忽略改为明确失败并要求模型重试，避免生成缺段文档。文档模块不再在解析失败日志中输出正文片段。
- 稳定版验证：`C:\Python314\python.exe -m pytest -q -p no:cacheprovider test_document_generator.py`，专项 12 项通过；稳定版离线回归 135 项通过；相关文件均通过 `py_compile` 和 `git diff --check`。既有 `test_sheets_optype.py` 因仍导入已不存在的 `auth.db.get_db` 无法收集，与本次修改无关。
- 建议迁移方式：按新版架构重新实现，不直接复制稳定版解析器或大型提示词。
- 新版落点建议：在文档工具适配器入口设置 schema validation，在副作用端口前设置 fail-closed 写入门禁；把结构错误定义为显式可重试 ToolResult，并以契约测试固定“解析失败绝不上传”和“只有成功回执才能声明完成”。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：该记录只描述稳定版变化，不表示新项目已经同步；新版还应评估使用类型化 DTO/JSON Schema 校验替代提示词容错。

### STABLE-20260911-02：DeepSeek V4.1 模型、长上下文、多模态与空白回复保护

- 日期：2026-09-11
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`e20bcb3`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：DeepSeek 官方直接接口的当前模型名更新为 `deepseek-flash`，模型具备 1M 上下文和图片输入能力；旧预设仍使用 V4 名称、上下文留空且视觉能力关闭。思考模型在最大输出额度耗尽时还可能只返回 `reasoning_content` 而没有 `content`，旧逻辑会把空字符串保存为助手回复。
- 用户可见行为：DeepSeek 新配置默认选择 `deepseek-flash`；设置页按模型显示 1,000,000 上下文、图片输入能力和建议输出上限。标准/思考模式的原有选择方式保持不变，自动思考强度按官方默认改为 `high`。旧 V4 Flash 名称继续可选兼容。思考请求若没有最终正文，系统自动关闭思考重试一次；仍失败时返回明确错误，不再出现只有 Token 统计、没有正文的空白消息。图片及扫描件可直接交给 `deepseek-flash` 识别，识图请求显式关闭思考以减少消耗。
- 涉及文件：`agent/assistant.py`、`api/app_new_routes.py`、`api/settings_new_routes.py`、`static/settings_new.html`、`core/file_parser.py`、`core/dashboard_service.py`、`app.py`、`test_admin_settings_new.py`、`test_tool_planner.py`、`test_dashboard.py`、`CHANGELOG.md`、`DEVLOG.md`
- 数据库变化：无；已有用户模型配置保持原值，不自动覆写 API Key、Base URL 或模型选择。用户在设置页选择并保存新模型后才持久化新名称和能力值。
- API 或事件协议变化：设置页 bootstrap 的内置模型条目新增 `supports_vision`、`context_window`、`max_output_tokens` 元数据；原字段和自定义模型格式保持兼容。聊天流事件不变，自动重试会如实累计一次额外模型请求及其 Token。
- 配置、环境变量或依赖变化：无新增依赖或环境变量。DeepSeek 新预设 Base URL 使用官方 `https://api.deepseek.com`；既有 `https://api.deepseek.com/v1` 配置继续可用。
- 权限、副作用和兼容性：模型别名只在发请求前移除 `-reasoning`，不改变其他厂商和 SCNet 的专用模型名。已知 DeepSeek Flash 能力由服务端识别，即使旧配置的视觉复选值为 false 也可用于图片解析。短文本合规/知识辅助调用显式关闭 DeepSeek 思考，避免新模型默认思考造成空结果和额外 Token。工具循环完整回传同一任务内每次模型响应的 `reasoning_content`（包括空字符串）；新用户轮次把旧的可见对话折叠为普通用户上下文，不伪造或持久化跨轮思维链，既满足 DeepSeek 工具请求结构，又避免数据库保存隐式推理和 Token 膨胀。
- 稳定版验证：DeepSeek/设置/智能体/仪表盘/聊天专项 57 项通过；稳定版离线回归 140 项通过；设置页 2 个 JavaScript 脚本块通过 Node 语法检查；相关 Python 文件通过 `py_compile`，`git diff --check` 无错误。回归仅有 1 条既存 Pydantic forward-reference 警告。
- 建议迁移方式：按新版模型能力注册表、模型适配器和运行时失败策略重新实现，不直接复制稳定版预设字典或大型工具循环。
- 新版落点建议：用类型化 ModelCapability/Profile 表达上下文、输出、多模态、工具和推理参数；在 DeepSeek adapter 内统一处理界面别名、thinking 参数、`reasoning_content` 续传和空正文降级；以契约测试固定“空正文绝不持久化”。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：该记录只描述稳定版变化，不表示新项目已同步。新项目迁移时应重新核对 DeepSeek 当时的官方模型名称、上下文和图像输入协议。

### STABLE-20260914-01：产品中文名称统一为“业务智能助手”

- 日期：2026-09-14
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`7579efa`
- 优先级：P2
- 新版状态：待评估
- 修改原因：产品已从多维表格操作扩展为覆盖业务协同的智能助手，原名称不能准确代表当前定位，且登录页、工作页和多个入口展示不一致。
- 用户可见行为：登录、注册、工作页、离线页、管理/设置/驾驶舱浏览器标题、PWA 安装名称、验证与重置邮件的发件人/标题/正文、MCP 展示名称及当前帮助文档统一使用“OpenNexus 业务智能助手”或“业务智能助手”。“WPS 多维表格”等具体功能名称不变。
- 涉及文件：`static/auth.html`、`static/app_new.html`、`static/offline.html`、`static/settings_new.html`、`static/admin_new.html`、`static/dashboard.html`、`static/service-worker.js`、`app.py`、`core/mcp_server.py`、`auth/email_sender.py`、`README.md`、`README_EN.md`、`docs/用户帮助.md`、`test_branding.py`、`test_pwa.py`、`CHANGELOG.md`。
- 数据库变化：无；用户配置、对话、账号和已存文件均不改名。
- API 或事件协议变化：`/api/mcp/info` 的 `name` 文本、MCP server 展示名及 `/manifest.json` 的 `name` 文本更新；端点、字段结构和机器标识保持不变。
- 配置、环境变量或依赖变化：无；Service Worker 缓存版本由 `opennexus-pwa-v4` 提升至 `opennexus-pwa-v5`，以更新离线页及 PWA 名称。
- 权限、副作用和兼容性：不改变登录验证、WPS 授权、工具、数据读写或权限逻辑。旧 PWA 安装名称可能需要浏览器刷新缓存，个别设备可能需重新安装快捷方式。历史发布记录保留原文。
- 稳定版验证：`C:\Python314\python.exe -m pytest -q -p no:cacheprovider` 运行 16 份离线回归测试文件，共 144 项通过；仅有 1 条既有 Pydantic forward-reference 警告。涉及的 Python 文件通过 `py_compile`，受影响页面脚本通过 Node 语法检查，`git diff --check` 无错误。
- 建议迁移方式：按新版架构集中实现品牌文案，不直接复制稳定版分散的 HTML 和运行时常量。
- 新版落点建议：统一产品品牌配置或资源文件，并为登录、工作页、PWA、邮件与 MCP 元数据增加契约测试；保留“WPS 多维表格”作为能力名。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：仅记录稳定版变更，不表示新项目已同步；稳定版不修改新项目目录。

### STABLE-20260915-01：驾驶舱改称“业务智能驾驶舱”

- 日期：2026-09-15
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`342ba4f`
- 优先级：P2
- 新版状态：待评估
- 修改原因：驾驶舱支持切换不同 WPS 业务文件，固定称作“部门驾驶舱”、把分析对象预设为部门，会让其他业务表格的展示与报告语境不准确。
- 用户可见行为：页面、浏览器标题、概览标题、仪表盘标题、PWA 快捷方式、帮助与对外驾驶舱技能统一使用“业务智能驾驶舱”；规则报告改用“当日业务事项”“业务模板”，模型分析提示词不再预设读者是部门负责人。旧规则快照在读取时仅转换已知的固定旧文案，真实文件名、表格字段、记录和历史 AI 分析原文保留。
- 涉及文件：`static/dashboard.html`、`static/service-worker.js`、`app.py`、`api/dashboard_routes.py`、`core/dashboard_service.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`README.md`、`README_EN.md`、`workbuddy-skills/opennexus-dashboard-analysis/SKILL.md`、`test_dashboard_branding.py`、`test_dashboard.py`、`test_app_new.py`、`test_branding.py`、`test_pwa.py`、`CHANGELOG.md`。
- 数据库变化：无表结构或写入逻辑变化；旧快照只在返回时显示新系统文案，不批量覆写历史数据。
- API 或事件协议变化：`/api/dashboard/data` 与 MCP `get_dashboard` 返回结构不变，概览 `title` 和规则报告固定文案更新；旧快照经展示层兼容处理。`/manifest.json` 的驾驶舱快捷方式 `name` 与应用 `description` 更新，字段结构和路径不变。
- 配置、环境变量或依赖变化：无；Service Worker 缓存版本由 `opennexus-pwa-v5` 升至 `opennexus-pwa-v6`，用于更新缓存的 PWA manifest。
- 权限、副作用和兼容性：不改变 WPS 授权、选表、数据读取、聚合口径、快照归属或四个既有视图。旧 AI 分析可能仍含原历史措辞，系统不擅自改写；用户可主动重新生成。此次仅泛化称谓，不代表新增任意表格自动建模能力。
- 稳定版验证：驾驶舱、工作页、PWA 和品牌专项 31 项通过；17 份离线回归测试文件共 148 项通过，仅有 1 条既有 Pydantic forward-reference 警告。相关 Python 文件通过 `py_compile`，驾驶舱内联脚本通过 Node 语法检查，`git diff --check` 无错误。
- 建议迁移方式：按新版架构重新实现统一品牌文案、报告提示词与历史快照显示兼容，不直接复制稳定版内联 HTML 或聚合服务。
- 新版落点建议：在驾驶舱展示模型或 DTO 中集中提供通用标题，在报告策略中使用与所选文件一致的业务上下文，并以契约测试固定“旧系统文案可显示兼容、用户原始字段和历史 AI 内容不被替换”。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：本记录只反映稳定版，不表示新项目已同步；未改动 `OpenNexus-Next`。

### STABLE-20260915-02：驾驶舱按任意业务表格实际字段生成概览

- 日期：2026-09-15
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`342ba4f`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：上次只泛化名称，概览仍假定选中表格包含每日进展、任务、项目，其他业务文件会出现没有意义的固定零值。
- 用户可见行为：概览遍历所选 WPS 多维表格文件的实际工作表，自动按字段生成记录数、数值汇总、分类分布、工作表明细预览和基础文字说明。只有识别到对应表时显示每日进展、任务或项目专用入口；切换文件回到概览。旧固定概览快照不再作为新概览复用。
- 涉及文件：`core/dashboard_generic.py`、`core/dashboard_generic_cache.py`、`core/dashboard_service.py`、`api/dashboard_routes.py`、`static/dashboard.html`、`test_dashboard_generic.py`、`test_dashboard_branding.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`README.md`、`README_EN.md`、`workbuddy-skills/opennexus-dashboard-analysis/SKILL.md`、`CHANGELOG.md`、`DEVLOG.md`。
- 数据库变化：无表结构迁移；复用 `dashboard_data_cache`，新增按用户/文件隔离的 `generic_index` 和 `generic_sheet:<sheet_id>` 数据种类。原任务/项目/日报缓存不删除。
- API 或事件协议变化：`/api/dashboard/data?view=overview` 的 JSON 外形兼容，新增 `analysis_version=2` 和 `available_views`，KPI、分布、明细、文字报告改为实际字段动态生成。`refresh=true` 对概览同步全部工作表，对专用页保持旧同步流程但检查失败回执。旧 `overview` 快照自动重算；直接读取快照前核验用户是否仍配置该文件。
- 配置、环境变量或依赖变化：无新增依赖或环境变量。
- 权限、副作用和兼容性：仅使用当前用户 WPS 授权和已配置文件，WPS 只读；缓存仍按用户/文件隔离。单表 5000 条安全上限，缺游标/超限显式标注不完整，不把已读取记录数当作总量。旧四种 `view` 参数及专用分析口径保留，但旧概览的固定指标不再使用。
- 稳定版验证：17 份核心离线测试文件共 156 项通过；含任意业务表、分页缺游标、接口总量不符、旧快照重算、缓存权限与页面视图联动专项。相关 Python 文件通过 `py_compile`，驾驶舱 2 段内联脚本通过 Node 语法检查，`git diff --check` 通过。仅有 1 条既有 Pydantic forward-reference 警告。未连接生产 WPS 做端到端验证；旧 `test_document_gen.py` 需额外异步 pytest 插件，与本次改动无关。
- 建议迁移方式：在新项目按类型化表结构探查、分页读取端口、聚合策略和按表类型启用专用视图重新实现，避免复制稳定版的字符串启发式。
- 新版落点建议：以每张表的完整性元数据作为指标可信度门禁；文件/用户权限在缓存命中前检查；任一工作表读取失败不得把部分文件冒充完整文件。增加任务、项目、非标准业务表及超大表的黄金任务对照。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：本记录只反映稳定版变化，不表示 `OpenNexus-Next` 已同步；本次未改动新项目。

### STABLE-20260915-03：驾驶舱空日期快照校验与按需读取

- 日期：2026-09-15
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`342ba4f`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：截图显示“部门事务”2026-09-14 每日进展为 0；本地缓存同日期已有 18 条且已有非空快照，说明不能把截图中的 0 当作 WPS 原始数据为空。旧逻辑可能长期复用历史空快照，切表延续前一文件日期，专用页首次加载还会同步不相关工作表。
- 用户可见行为：缓存数据比快照更新时自动重建；历史 0 条快照不再永久有效，过期后仅核对选中日期；非空快照在数据源未变化时直接复用。驾驶舱 API 请求不复用浏览器缓存响应。切换文件回到概览并重置日期；首次进入每日进展优先选该文件已缓存的最新日期，手动选择日期仍受尊重。专用页优先复用通用概览完整工作表缓存，需同步时只读当前页相关工作表；首次读取未缓存文件仍需等待 WPS。
- 涉及文件：`core/dashboard_cache.py`、`core/dashboard_service.py`、`api/dashboard_routes.py`、`static/dashboard.html`、`test_dashboard_speed.py`、`test_dashboard_generic.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`CHANGELOG.md`。
- 数据库变化：无表结构迁移；仅沿用按用户和文件隔离的驾驶舱原始缓存与快照。空快照可能被新的核验结果覆盖，不改 WPS 原始记录。
- API 或事件协议变化：现有驾驶舱端点、请求参数和响应外形不变；`refresh=true` 的专用页现在只同步该页和可选人员表，日报采用日期定向读取。日期列表可从通用概览缓存补充。
- 配置、环境变量或依赖变化：无。
- 权限、副作用和兼容性：仍先检查当前用户对所选文件的配置权限，缓存按用户/文件隔离；WPS 仅只读。空日期核验失败会报错，不伪造非零值；未缓存文件仍需首次同步。旧的非空快照、专用页参数和手动日期操作保持兼容。
- 稳定版验证：18 份离线测试文件共 162 项通过（含 6 项本轮新增回归），仅有 1 条既有 Pydantic forward-reference 警告；相关 Python 文件通过 `py_compile`，驾驶舱内联 JavaScript 通过 Node 语法检查。未连接生产 WPS 做端到端验证。
- 建议迁移方式：在新项目按类型化缓存元数据和按视图数据依赖重新实现，避免直接复制稳定版的快照时间比较和工作表名称启发式。
- 新版落点建议：为源数据版本/时间戳、空结果有效期、用户/文件权限、日期选择与视图级读取建立独立契约测试；用真实 WPS 沙箱验证历史补录、跨文件切换和冷/热加载耗时。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：仅记录稳定版修复，不表示新项目已经同步；本次未修改 `OpenNexus-Next`。

### STABLE-20260915-04：WPS 非数据工作表兼容与切表失败隔离

- 日期：2026-09-15
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`342ba4f`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：“技术质量月报”包含 `xlEtFlexPaperSheet` 自由纸张说明页，其 records 接口返回 HTTP 400、`Sheet not found`；“部门事务”包含 `xlDbDashBoardSheet` 仪表盘页，其 records 接口返回 HTTP 500。WPS schema 会返回这些页面，稳定版原先把它们都当作数据工作表，导致整个概览失败。失败后前端又重新显示上一文件的隐藏内容，造成跨文件误导。
- 用户可见行为：schema 提供类型时，通用概览只读取 `xlEtDataBaseSheet` 数据表；WPS 仪表盘、自由纸张和其他非数据页会安全跳过，并在“数据提示”和总体概况中列出。切换文件时立即清空旧页面语境、隐藏旧仪表盘；新文件失败时只显示错误，不再露出上一文件数据。
- 涉及文件：`core/dashboard_generic_cache.py`、`core/dashboard_generic.py`、`core/dashboard_service.py`、`static/dashboard.html`、`test_dashboard_generic.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`CHANGELOG.md`、`DEVLOG.md`。
- 数据库变化：无表结构迁移；通用缓存索引版本由 2 升至 4，增加 `skipped_sheets` 元数据并升级工作表类型规则。旧索引会自动重新同步；用户配置、快照和 WPS 原始数据不改。
- API 或事件协议变化：`/api/dashboard/data?view=overview` 外形保持兼容，概览 `insights` 可能新增跳过页面提示；KPI“工作表数”改为“可分析工作表”，仅统计记录型工作表。专用视图只基于数据工作表启用。
- 配置、环境变量或依赖变化：无。
- 权限、副作用和兼容性：WPS 操作只读；类型缺失的旧响应仍尝试读取，明确提供类型时只接受数据库表，其他类型保守跳过。若 WPS 未来新增记录型工作表类型，需要在适配器登记后启用。真正的数据工作表读取失败仍阻止生成完整概览，避免把缺失数据伪装成完整结果。
- 稳定版验证：驾驶舱专项 27 项通过；18 份离线回归测试文件共 164 项通过，仅有 1 条既有 Pydantic forward-reference 警告。使用当前用户本地 WPS 授权只读实测：“技术质量月报”读取 2 张数据表共 24 条，跳过自由纸张说明页；“部门事务”读取 10 张数据表共 2682 条，跳过仪表盘页。未输出记录正文，未修改 WPS 数据。
- 建议迁移方式：在新项目按 WPS schema 的工作表类型能力映射重新实现，不直接复制稳定版字符串判断。
- 新版落点建议：为数据表、自由纸张、未来未知类型及真实数据表 HTTP 失败建立 adapter 契约；前端使用文件请求身份绑定展示状态，禁止错误回退时跨文件显示旧 payload。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：仅记录稳定版修复，不表示新项目已经同步；本次未修改 `OpenNexus-Next`。

### STABLE-20260915-05：大模型声明式设计任意业务驾驶舱

- 日期：2026-09-15
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`342ba4f`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：仅按字段类型固定生成记录数、数值汇总和分类分布，无法理解任意业务表的字段语义；但让模型直接生成 HTML、代码或统计结果又会带来不可控执行、虚构数值和数据泄露风险。
- 用户可见行为：“整体情况”默认显示无需模型的基础概览。用户点击“重新设计与分析”并确认后，当前账号配置的大模型根据真实工作表及字段 schema 生成专属指标、图表和明细方案；方案通过后立即呈现并缓存。每日进展、任务分析和项目分析继续作为识别到既有业务表后的专用视图。
- 涉及文件：`core/dashboard_planner.py`、`core/dashboard_service.py`、`static/dashboard.html`、`test_dashboard_planner.py`、`test_dashboard_generic.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`CHANGELOG.md`、`DEVLOG.md`。
- 数据库变化：无表结构迁移；复用 `dashboard_data_cache`，新增 `data_kind=dashboard_design` 缓存，按用户、文件保存设计版本、schema 指纹、模型指纹、状态和已校验方案。原始 WPS 数据不改。
- API 或事件协议变化：`/api/dashboard/data?view=overview` 向后兼容地增加 `dashboard_design_version` 和 `dashboard_design`；`ai_summary=true` 在整体情况中同时表示用户确认重新设计和生成分析。既有字段保持不变。
- 配置、环境变量或依赖变化：无；继续使用当前账号已有大模型配置和 OpenAI 兼容客户端。
- 权限、副作用和兼容性：首次普通打开不调用外部模型；只有用户在既有确认框确认后才允许调用。设计请求只包含文件名、数据工作表名称、字段名称/类型、已读取数量、完整性和跳过页名称，不包含记录正文；文字分析仍按确认框说明使用当前页面汇总。模型只能选择真实 sheet/field 和 `count_rows`、`count_nonempty`、`distinct_count`、`sum`、`average` 白名单操作，不能提供代码、SQL、公式或最终数值；聚合和图表数据全部由系统从当前用户本地缓存计算。模型失败、未配置或方案无效时保留基础概览。大表部分数据指标明确标注“已读取”。
- 稳定版验证：新增 6 项规划器测试，覆盖 schema-only 输入、显式同意门禁、模型禁用时零调用、缓存复用、非法字段/操作拒绝和本地聚合；驾驶舱相关 33 项通过，19 份稳定版离线回归测试文件共 170 项通过，仅有 1 条既有 Pydantic forward-reference 警告。未执行真实外部模型设计调用，未修改 WPS 数据。
- 建议迁移方式：在新项目按新版架构重新实现，不直接复制稳定版模块间调用。建立类型化 `DashboardSpec`、schema catalog、planner、validator、local evaluator、renderer 和 versioned cache 边界。
- 新版落点建议：把用户同意范围、模型数据最小化、字段能力/数据类型、聚合精度、部分读取标识、设计版本失效、租户隔离和方案审计设为契约；用多类脱敏业务表做黄金测试，验证模型换代时页面仍可复现。专用视图应作为可插拔模板，与通用模型设计共用同一声明式协议。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：稳定版只记录行为基线；本次未修改 `OpenNexus-Next`，不表示新项目已经同步。

### STABLE-20260915-06：不定数量驾驶舱图表响应式均衡排列

- 日期：2026-09-15
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`342ba4f`
- 优先级：P2
- 新版状态：待迁移
- 修改原因：模型可为不同业务表生成不定数量的分布图，旧页面把首图固定在顶部、其余图全部放入右侧 `aside` 纵向堆叠；左侧指标块较短时会留下大面积空白。
- 用户可见行为：顶部主图与 KPI 布局保持不变；其余指标、分布图和数据提示进入同一自动适配网格，在可用宽度内自动分列和换行，窄屏自动单列。布局不依赖图表标题、字段或数量。
- 涉及文件：`static/dashboard.html`、`test_dashboard_generic.py`、`docs/用户帮助.md`、`CHANGELOG.md`、`DEVLOG.md`。
- 数据库变化：无。
- API 或事件协议变化：无；只调整现有响应内容的前端排列。
- 配置、环境变量或依赖变化：无。
- 权限、副作用和兼容性：不改变模型设计、WPS 读取、聚合、缓存及用户隔离；使用标准 CSS Grid 和 `display: contents`，700px 以下明确回退为单列。现有 DOM ID 和渲染函数保持不变。
- 稳定版验证：驾驶舱页面回归增加响应式网格、自动分列和侧栏展平断言；Python 驾驶舱专项、内联 JavaScript 解析及差异检查通过。
- 建议迁移方式：新版使用独立的可复用 `DashboardCardGrid` 组件实现，不直接复制稳定版内联样式。
- 新版落点建议：组件接受不定数量的 KPI、chart、hint 卡片，按容器宽度计算列数；增加 0、1、2、3、6 张图以及手机、平板、桌面视口的视觉回归。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：本次只修改稳定版页面和迁移账本，未修改 `OpenNexus-Next`。

## 8. 最终审计

正式切换 OpenNexus Next 前，必须同时完成：

1. 比较共同基线至稳定版最终提交的全部 Git commit；
2. 比较受影响文件，而不是机械合并大型旧文件；
3. 核对数据库迁移、API、配置、环境变量和依赖；
4. 将本账本所有记录处理为 `已验收` 或有用户批准的 `不适用`；
5. 执行脱敏黄金任务和端到端回归；
6. 设定短暂稳定版功能冻结窗口，完成最后一次增量审计。

### STABLE-20260915-07：驾驶舱字段语义门禁、关联值解码与核心指标精简

- 日期：2026-09-15
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`342ba4f`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：模型能从 schema 选中真实字段，但旧校验只验证字段存在，可能把数字形态的 WPS 成员 ID、配置选择项当作平均值；WPS 关联字段和层级字段的缓存原值又会直接显示 `Gu`、`Bx`、`Common` 等内部标记。指标数量过多时，“整体情况”也会退化为字段清单。
- 用户可见行为：整体情况最多显示 4 个 KPI，KPI 与补充指标合计最多 6 个，自动去重并优先要求规模、进度、质量、成本、风险、人效等决策指标。ID、编号、账号、工号、证件、电话、邮编等标识字段不会被作为指标、图表或自动明细；求和和平均只允许 schema 明确声明的数值字段或数值公式。单向/双向关联值按同一 WPS 文件缓存中的记录名称解码，层级字段显示业务路径并隐藏内部 `type`；无法可靠解析的内部关联值不作为业务名称展示。
- 涉及文件：`core/dashboard_field_semantics.py`、`core/dashboard_generic.py`、`core/dashboard_planner.py`、`core/dashboard_service.py`、`test_dashboard_planner.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`CHANGELOG.md`、`DEVLOG.md`、`docs/NEXT_MIGRATION_CHANGELOG.md`。
- 数据库变化：无表结构迁移；复用现有通用工作表缓存和 `dashboard_design` 缓存。设计版本由 1 升至 2，旧设计和旧概览快照自动失效后重新生成。
- API 或事件协议变化：`/api/dashboard/data?view=overview` 响应外形不变，`dashboard_design_version` 从 1 升至 2；指标数量上限和显示标签语义收紧。
- 配置、环境变量或依赖变化：无。
- 权限、副作用和兼容性：只读取当前用户、当前文件的本地 WPS 缓存，不新增 WPS 请求或写操作，不跨文件解析关联值。只有能够在同一文件内唯一映射的记录 ID 才显示业务名称；重名或缺失映射安全留空。日期字段仍可用于分类和明细，不因标识门禁被误删。部署后旧设计会回到基础概览，用户点击一次“重新设计与分析”即可生成并保存版本 2 方案。
- 稳定版验证：驾驶舱专项 35 项通过；19 份稳定版离线回归测试共 172 项通过，仅有 1 条既有 Pydantic forward-reference 警告。新增回归覆盖 WPS 成员 ID 和数字选择项拒绝、关联记录 ID 还原人名、Cascade 层级隐藏 `Common`、规划输入最小化、指标数量上限与去重及版本 2 缓存。
- 建议迁移方式：按新版架构重新实现类型化字段能力和显示值解析，不直接复制稳定版正则与模块依赖。
- 新版落点建议：建立 `FieldSemantic`/`DisplayValueResolver` 边界；schema 类型决定允许的聚合，字段角色决定管理价值，relation adapter 负责跨表显示名；以标识字段、数字文本、布尔选择、联系人、关联、层级、重名和缺失映射建立契约测试。驾驶舱规格应显式限制摘要指标数量并支持设计版本失效。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：本记录只反映稳定版，不表示 `OpenNexus-Next` 已同步；本次未修改新项目。

### STABLE-20260916-01：模型相对日期与星期权威日历注入

- 日期：2026-09-16
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：`0380904`
- 优先级：P1
- 新版状态：待迁移
- 修改原因：系统原先只注入当前北京时间和当天星期，模型需要自行推算“昨天”等相对日期。模型在查询到 2026-09-15 的正确记录后，疑似套用了 2025 年日历并错误称为周一，导致报告日期语义不可靠。
- 用户可见行为：每轮对话同时获得前天、昨天、今天、明天的完整年份、日期和星期，并明确要求直接使用系统日历、禁止自行换算。相同日历同时放入 system 提示词和最后一条用户消息上下文，降低不同模型忽略系统时间的概率。例如 2026-09-16 周三时，“昨天”固定为 2026-09-15 周二。
- 涉及文件：`agent/assistant.py`、`test_time_context.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`CHANGELOG.md`、`DEVLOG.md`、`docs/NEXT_MIGRATION_CHANGELOG.md`。
- 数据库变化：无。
- API 或事件协议变化：无；仅增强发送给模型的上下文文本。
- 配置、环境变量或依赖变化：无。
- 权限、副作用和兼容性：使用固定 UTC+8 北京时区，不依赖服务器系统时区；兼容无时区和有时区的测试时间。只增加少量提示词 Token，不改变 WPS 查询、写入、提醒入库或用户权限。相对日期范围目前固定为前天、昨天、今天、明天；其他日期仍应使用明确年月日或工具结果。
- 稳定版验证：时间上下文、工具规划与工具治理专项 31 项通过；20 份稳定版离线回归测试共 174 项通过，仅有 1 条既有 Pydantic forward-reference 警告。固定时钟测试覆盖 2026-09-16 周三及前后相对日期，相关 Python 文件通过编译。
- 建议迁移方式：按新版架构重新实现统一 `Clock`/`RelativeDateContext` 服务，不直接复制稳定版提示词拼接。
- 新版落点建议：所有智能体、提醒解析和报告生成共享同一个注入时钟；以固定时钟测试跨年、月末、闰年、夏令时无关性和中文相对日期，业务输出引用系统解析后的 ISO 日期而不是模型自行计算。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：本记录只反映稳定版，不表示 `OpenNexus-Next` 已同步；本次未修改新项目。

### STABLE-20260916-02：提醒过期门禁与有限重试

- 日期：2026-09-16
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：待提交
- 优先级：P1
- 新版状态：待迁移
- 修改原因：提醒队列原先只按 `remind_at <= 当前时间` 取数，失败后长期保留并以最长 60 分钟间隔无限重试；个人微信桥接恢复或服务重启时会把历史积压提醒重新发送。
- 用户可见行为：个人微信短时断线仍会退避重试；普通定时提醒超过原定时间 30 分钟、提前提醒已经晚于独立事项时间，或累计发送失败达到 5 次后自动过期，不再补发。过期原因可在投递审计中追溯。
- 涉及文件：`app.py`、`auth/db.py`、`test_reminders.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`CHANGELOG.md`、`DEVLOG.md`、`docs/NEXT_MIGRATION_CHANGELOG.md`。
- 数据库变化：无表结构迁移；复用 `reminders.retry_count`、`reminders.remind_at`、`reminders.event_at` 与既有 `reminder_delivery_log`。过期记录先以 `status=expired` 写入审计，再从待发送队列删除。
- API 或事件协议变化：无外部 API 变化；`mark_reminder_failed` 内部返回值由无返回改为累计失败次数，现有忽略返回值的调用保持兼容。
- 配置、环境变量或依赖变化：无；稳定版内置最大迟到 30 分钟、最大失败 5 次。
- 权限、副作用和兼容性：只处理当前提醒队列，不修改 WPS 数据、用户绑定或已经成功的投递记录。部署重启后，既有过期积压在下一次符合扫描条件时记录为过期并删除，不调用任何通知通道；仍处于 30 分钟窗口且未达到次数上限的提醒继续正常重试。
- 稳定版验证：提醒与微信专项 43 项通过；20 份稳定版离线回归测试共 180 项通过，仅有 1 条既有 Pydantic 警告和 1 条当前沙箱的 pytest 缓存目录警告。相关 Python 文件通过编译。
- 建议迁移方式：在新项目按新版边界重新实现，不直接复制稳定版 lifespan 调度代码。
- 新版落点建议：建立类型化 `ReminderPolicy`、`ReminderRepository` 和幂等 `DeliveryWorker`；把补发窗口、事项截止、最大次数、租户隔离、并发领取、终态审计及可配置策略写入契约测试。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：本记录只反映稳定版，不表示 `OpenNexus-Next` 已同步；本次未修改新项目。

### STABLE-20260916-03：个人微信多部署单一连接与桥接隔离

- 日期：2026-09-16
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：待提交
- 优先级：P1
- 新版状态：待迁移
- 修改原因：服务器稳定版、本地稳定版和 V2 曾共用 `~/.wechat-claude-code/accounts`、相同低位端口，并扫描 3001–3010 寻找桥接，导致一个系统可能杀掉、重启或借用另一个系统的个人微信进程；Node 限流重试和模型二次调用还会让测试消息等待很久并可能重复发送。
- 用户可见行为：每个 OpenNexus 部署只使用自己的微信凭据目录、端口段和实例标识。同一个个人微信在另一个系统重新扫码后，原连接失效，原系统停止自动恢复并要求重新扫码；不会借用另一个系统的在线桥接。交互发送限流时快速失败，同一智能体轮次最多尝试发送一次。
- 涉及文件：`app.py`、`agent/assistant.py`、`core/wechat_delivery.py`、`core/wechat_instance.py`、`core/mcp_server.py`、`.env.example`、`wechat-claude-code-main/src/config.ts`、`src/logger.ts`、`src/main.ts`、`src/wechat/accounts.ts`、`src/wechat/api.ts`、`src/wechat/send.ts`、`test_wechat_delivery.py`、`test_wechat_instance.py`、`test_wechat_qr.py`、`docs/用户帮助.md`、`docs/User-Guide-EN.md`、`CHANGELOG.md`、`DEVLOG.md`、`docs/NEXT_MIGRATION_CHANGELOG.md`。
- 数据库变化：无。
- API 或事件协议变化：桥接 `GET /health` 新增 `instanceId`；桥接调用 `POST /api/weixin/session_expired` 时新增 `instanceId`，稳定版拒绝不属于当前实例的回调，并在已配置内部令牌时校验回调令牌；凭据删除只接受安全的账号文件名；`POST /local/send` 在限流或发送错误时立即返回，不再在 Node 内部等待 10/20/40 秒重试。现有外部个人微信通知 API 路径不变。
- 配置、环境变量或依赖变化：新增可选 `OPENNEXUS_WECHAT_INSTANCE_ID`、`OPENNEXUS_WECHAT_DATA_DIR`、`OPENNEXUS_WECHAT_PORT_BASE`、`OPENNEXUS_WECHAT_PORT_COUNT`、`OPENNEXUS_INTERNAL_URL`。没有新增依赖；桥接源码更新后必须执行 `npm run build`。
- 权限、副作用和兼容性：旧公共目录中的微信凭据不自动迁移，升级后每个需要微信能力的部署必须重新扫码一次。当前实例只清理自己的失效凭据，不触碰其他系统目录。跨服务器接管依赖微信服务使旧 token 失效；若上游未来允许同一账号同时维持多个 token，新版还需用中心租约实现严格全局单活。
- 稳定版验证：Python 语法检查通过；TypeScript `npm run build` 通过；微信实例、投递、二维码、监护、提醒和工具路由专项 75 项通过；排除 4 个既有脚本式或失效测试入口后，其余稳定版自动化回归 187 项通过。未修改或启动 V2，未部署服务器。
- 建议迁移方式：按新版架构重新实现，不直接复制稳定版全局变量、lifespan 监护或端口映射代码。
- 新版落点建议：建立类型化 `WechatConnectionLease`、部署实例身份、加密凭据存储和桥接进程适配器。连接租约应以租户与微信账号为键，支持原子接管、撤销旧持有者、心跳、终态审计、幂等发送和跨主机协调；对 `/health`、接管事件和发送回执建立契约测试。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：本记录只反映稳定版，不表示 `OpenNexus-Next` 已同步；本次未修改新项目。

### STABLE-20260917-01：个人微信会话激活、上下文持久化与主动发送真值门禁

- 日期：2026-09-17
- 稳定版分支：`codex/main-release-20260827`
- 稳定版提交：待提交
- 优先级：P1
- 新版状态：待迁移
- 修改原因：个人微信桥接原本以“微信入站、智能体回复”为主，入站消息虽然携带 `context_token`，稳定版却只把它放在内存变量中；`POST /local/send` 主动发送始终传空令牌，并把部分仅受理或无消息编号的响应报告为成功，导致扫码后系统显示 `ok:true` 而手机未收到，只有用户先从微信发一句后才可能恢复。
- 用户可见行为：设置页现在区分“桥接已连接”和“主动消息已激活”。扫码后用户需从微信向助手发送任意一句话完成首次激活；最新上下文按桥接账号和联系人持久化，进程重启后可以恢复，且不会因为长时间没有微信入站而由 OpenNexus 主动过期。未激活时，设置页测试消息和智能体明确要求的即时个人微信最多暂存 30 分钟，收到该联系人的首条微信后自动补发；界面和工具会明确说明“尚未发送”，不再误报成功。定时提醒和业务 webhook 不进入该队列，继续遵守各自的过期与重试规则。
- 涉及文件：`app.py`、`agent/assistant.py`、`core/mcp_server.py`、`core/wechat_delivery.py`、`static/settings_new.html`、`wechat-claude-code-main/src/main.ts`、`src/wechat/api.ts`、`src/wechat/send.ts`、`src/wechat/outbound-state.ts`、`src/tests/outbound-state.test.ts`、`test_wechat_delivery.py`、`test_wechat_qr.py`、中英文帮助、`CHANGELOG.md`、`DEVLOG.md`。
- 数据库变化：无；桥接在当前部署的 `WCC_DATA_DIR/outbound-state/<accountId>.json` 保存每联系人最新上下文和短期待发消息，非 Windows 系统沿用现有存储器的 0600 文件权限。
- API 或事件协议变化：桥接 `GET /health` 新增 `activated`、`contextUpdatedAt`、`pendingCount`；`POST /local/send` 新增可选 `queueIfInactive`，未激活返回 `202/409`、`code=wechat_activation_required`，已保存上下文遭遇模糊限流或临时波动返回 `202/409`、`code=wechat_send_deferred` 且不清除上下文，未取得消息编号返回 `502`、`code=delivery_unconfirmed`，确认成功时返回 `messageId`。设置页状态与测试接口透传激活和暂存语义。
- 配置、环境变量或依赖变化：无新增依赖或环境变量；桥接 TypeScript 修改后必须执行 `npm run build`，部署主程序和桥接均需重启。
- 权限、副作用和兼容性：上下文令牌只在本部署、同一 bot 账号、同一联系人范围内使用，不写日志、不跨账号复用，也不设置静默超时；每次微信入站只会刷新它。只有整个登录会话明确过期或被另一部署接管时才清除；上游 `ret=-2` 同时可能表示限流或临时状态波动，因此不得据此删除上下文。暂存仅对调用方显式启用的交互消息生效，按接收人和正文去重，最多保存 50 条且 30 分钟自动清理；定时提醒默认不暂存，避免与提醒数据库重试叠加或补发历史消息。扫码后第一次主动发送仍受上游个人微信协议约束，需要一次微信入站激活。
- 稳定版验证：TypeScript 编译通过；Node 会话状态测试 3 项通过；23 份稳定版离线 Python 回归共 191 项通过，另有 1 条既有 Pydantic forward-reference 警告；最终微信、设置页和实例隔离专项 38 项通过；相关 Python 文件通过编译，设置页 3 个内联脚本通过 Node 语法解析，`git diff --check` 通过。新增测试覆盖上下文跨重启恢复、账号/联系人隔离、待发去重与过期、连接但未激活状态、202 暂存回执及源码不再传空上下文。既有脚本式 `test_wps_api.py` 在 Python 3.14 下会关闭 pytest 捕获流，未纳入离线回归，与本次微信修改无关。
- 建议迁移方式：按新版架构重新实现，不直接复制稳定版 Node 进程内 HTTP 服务。
- 新版落点建议：建立类型化 `WechatConversationCapability`、加密上下文存储、`DeliveryReceipt` 和带 TTL/幂等键的交互待发箱；把“连接、激活、已受理、已确认、未知、失败”建模为不同状态，并以官方主动通知通道承接必须保证送达的业务消息。
- 新版实现提交：待定
- 新版验收证据：待定
- 备注：本记录只反映稳定版，不表示 `OpenNexus-Next` 已同步；本次未修改新项目。
